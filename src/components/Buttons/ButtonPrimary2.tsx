import React from "react";

const ButtonPrimary2 = ({ children }: { children: React.ReactNode }) => {
  return (
    <button className="button-style2  block mx-auto mt-2">{children}</button>
  );
};

export default ButtonPrimary2;
